One-click scan: 
```bash
git clone https://github.com/musheer090/kube-hunter-automator.git && cd kube-hunter-automator && chmod +x kube-hunter.sh && ./kube-hunter.sh && cd .. && rm -rf kube-hunter-automator
```
